import React, { useEffect } from 'react'

const Logout = () => {
  useEffect(() => {
    Logout();
  }, []);

  const logout = () => {
    
  }
  return (
    <div>Logout</div>
  )
}

export default Logout